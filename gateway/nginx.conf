events {}
http {
    client_max_body_size 50M;

    server {
        listen 80;
        location /graphql {
            proxy_pass http://backend:4000/;
        }

        location / {
            proxy_pass http://frontend:3000;
        }
    }
}
